<section class="container">
  <h3 class="genre-name">{{genre.name}}</h3>
  <br>
  <h4 class="text">Click Song for Preview</h4>
  <section class="flex-row justify-center">
    {{#each genre.songs as |song|}}
    {{> song-details genreName=../genreName}}
    {{/each}}
  </section>
</section>