<section class="genre">
  <div class="song-details">
    <h3 class="text">{{song.title}}</h3>
    <p class="text">Album: {{song.album}}</p>
    <p class="text">By: {{song.artist}}</p>
    {{{ song.embed }}}
    {{#if isLiked}}
    <button value="{{song.id}}" class="btn btn-primary like-button">Liked!</button>
    {{else}}
     <button value="{{song.id}}" class="btn btn-primary like-button">Like Song?</button>
     {{/if}}
  </div>
</section>

<script src="/js/like.js"></script>
<script src="./js/embed.js"></script>